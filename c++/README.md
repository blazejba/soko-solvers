## Installation

### dependencies
On Linux:  
`sudo apt-get install cmake libeigen3-dev`
  
On MacOS:
`brew install cmake libeigen3-dev`

### building 
`mkdir build`  
`cd build`  
`cmake ../src`  
`cmake --build .`

## Usage
`...`

